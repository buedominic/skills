{
  "$comment": "Für Codex nach <projekt>/.codex/workflow.config.json, für Claude nach <projekt>/.claude/workflow.config.json kopieren. Alle Felder optional — der Skill leitet Fehlendes aus AGENTS.md/CLAUDE.md/package.json ab.",
  "specsDir": "docs/specs",
  "plansDir": "docs/plans",
  "defaultBranch": "main",
  "maxReviewRounds": 5,
  "models": {
    "$comment": "Optional. Werte: 'inherit' (= Runtime-Konfiguration) oder ein von der jeweiligen Runtime unterstützter Modell-Alias. effort nur verwenden, wenn das Dispatch-Tool ihn unterstützt. reviewer='codex' bevorzugt den nativen Codex-Subagenten bzw. in Claude das Codex-MCP; reviewer='claude' bleibt in Claude.",
    "reviewer": "auto",
    "reviewerModel": "inherit",
    "reviewerEffort": "high",
    "implementerModel": "inherit",
    "implementerEffort": "medium"
  },
  "verifyCommands": [
    "npx tsc --noEmit",
    "npm run test:unit",
    "npm run test:dom",
    "npm run test:integration",
    "npm run build"
  ],
  "devServer": {
    "command": "npm run dev",
    "port": 3000,
    "healthUrl": "http://localhost:3000/api/health",
    "appMarker": "my-app"
  }
}
